version: '3.6'

services:
  db:
    container_name: psqlDatabase
    image: postgres
    restart: on-failure
    ports:
      - 5432:5432
    env_file:
      - .env
    volumes:
      - db-data:/var/lib/postgresql/data/

volumes:
  db-data: